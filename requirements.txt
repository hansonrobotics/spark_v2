# SPARK v2 — Dependencies
# Install: pip install -r requirements.txt

# Core runtime
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
aiosqlite>=0.20.0

# Used by microservice layer (optional for dev)
# neo4j>=5.0.0
# redis>=5.0.0
# pydantic>=2.0.0
